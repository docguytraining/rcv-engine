{
  "name": "rcv-engine",
  "version": "1.0.3",
  "description": "Zero-dependency TypeScript library for tabulating ranked-choice elections (IRV and STV) with a complete, auditable result.",
  "keywords": [
    "rcv",
    "ranked-choice",
    "voting",
    "irv",
    "instant-runoff",
    "stv",
    "single-transferable-vote",
    "tabulation",
    "election",
    "audit"
  ],
  "license": "AGPL-3.0-only",
  "author": "Paul Pehrson",
  "repository": {
    "type": "git",
    "url": "https://github.com/docguytraining/rcv-engine.git"
  },
  "homepage": "https://github.com/docguytraining/rcv-engine#readme",
  "bugs": {
    "url": "https://github.com/docguytraining/rcv-engine/issues"
  },
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/types/index.d.ts",
      "import": "./dist/esm/index.js",
      "require": "./dist/cjs/index.js"
    }
  },
  "main": "./dist/cjs/index.js",
  "module": "./dist/esm/index.js",
  "types": "./dist/types/index.d.ts",
  "files": [
    "dist",
    "src",
    "docs",
    "CHANGELOG.md"
  ],
  "scripts": {
    "build": "npm run build:esm && npm run build:cjs && npm run build:types && npm run build:cjs:fixup",
    "build:esm": "tsc -p tsconfig.esm.json",
    "build:cjs": "tsc -p tsconfig.cjs.json",
    "build:types": "tsc -p tsconfig.types.json",
    "build:cjs:fixup": "node scripts/fix-cjs.mjs",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:pack": "node scripts/test-pack.mjs",
    "prepublishOnly": "npm run clean && npm run typecheck && npm run build && npm test && npm run test:pack"
  },
  "devDependencies": {
    "@types/node": "^25.5.2",
    "fast-check": "^4.6.0",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
